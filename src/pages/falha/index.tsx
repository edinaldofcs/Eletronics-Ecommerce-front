import type { NextPage } from "next";

const Fail: NextPage = () => {
  
  
  return (
    <div className="h-96 flex items-center justify-center">
      Falha
    </div>
  );
};

export default Fail;
